{
  "name": "phonebookbackend",
  "version": "0.0.1",
  "description": "Backend for Phonebook App",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd  /Users/jaredklopstein/Desktop/fullstackopen/part2/phonebook && npm run build && cp -r build /Users/jaredklopstein/Desktop/fullstackopen/part3",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",    
    "logs:prod": "fly logs"
  },
  "author": "Jared Klopstein",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.21"
  }
}
